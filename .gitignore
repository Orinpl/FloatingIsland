# 仓库根级 .gitignore —— 只放 OS / 编辑器 / 通用垃圾。
# Unity 工程自身的忽略规则在 FloatingIsLand/.gitignore
# （Unity 模板的规则用 / 锚定工程根，必须放在工程目录里才生效）

# ---------- 操作系统 ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
# Unity 会给 .lnk 生成 meta；本体不入库，meta 也别留成孤儿
*.lnk.meta

# ---------- 编辑器 / IDE ----------
.vs/
.idea/
*.swp
*~

# VS Code：共享的配置可提交，个人层不提交
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# ---------- 通用 ----------
*.log
*.bak
*.orig
*.rej
node_modules/
__pycache__/
*.py[cod]

# 大体积压缩包默认不入库，确需提交时用 git add -f
*.zip
*.rar
*.7z

# ---------- 项目 ----------
# 美术生成管线的中间产物不入库：ArtGen/ 里是 2K 原图、4K 贴图版 FBX 与任务状态，
# 交付物已压缩后落在 Assets/Res/。流水线脚本本身在 FloatingIsLand/Tools/ArtGen/（入库）。
FloatingIsLand/ArtGen/

# 注意：Assets/Res/ 必须入库。Assets/Resources/Prefab 下的 Prefab 只是壳，
# 网格与材质全靠 GUID 指向 Assets/Res/ 下的 fbx 与 mat；把 Res 排除掉，
# 别人拉下来会得到一堆 Missing Mesh 的空 GameObject，而且不报任何错。
